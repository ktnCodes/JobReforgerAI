"""
Scoring package — organized access to ATS and HR scoring engines.

Usage:
    # New (organized) imports
    from scoring.ats import calculate_ats_score, detect_domain
    from scoring.hr import calculate_hr_score_from_text, result_to_dict

    # Legacy imports still work (backward compatible)
    import ats_scorer
    import hr_scorer
"""
